threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const counters=document.querySelectorAll('[data-count]');counters.forEach(el=>{const target=+el.dataset.count,suffix=el.dataset.suffix||'';let n=0;const go=()=>{if(reduced){el.textContent=target+suffix;return}const id=setInterval(()=>{n++;el.textContent=n+suffix;if(n>=target)clearInterval(id)},120)};go()});
const gauges=document.querySelectorAll('.gauge');const gaugeObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.style.setProperty('--v',e.target.dataset.value)}),{threshold:.4});gauges.forEach(g=>gaugeObs.observe(g));
const seq=['> connect --remote-session','✓ user authenticated','✓ connectivity checked','✓ issue isolated','✓ fix applied','✓ service restored','✓ resolved'];const terminal=document.getElementById('terminalText');let li=0,ci=0;function type(){if(reduced){terminal.innerHTML=seq.map((s,i)=>'<div'+(i===seq.length-1?' class="green"':'')+'>'+s+'</div>').join('');return}if(li>=seq.length)return;let lines=terminal.innerHTML.split('</div>').filter(Boolean);let current=seq[li].slice(0,ci++);lines[li]='<div'+(li===seq.length-1?' class="green"':'')+'>'+current;terminal.innerHTML=lines.map(x=>x.includes('<div')?x+'</div>':'<div>'+x+'</div>').join('');if(ci<=seq[li].length)setTimeout(type,20);else{li++;ci=0;setTimeout(type,180)}}setTimeout(type,reduced?0:1100);
const floatCard=document.getElementById('floatCard');addEventListener('scroll',()=>{if(!reduced&&innerWidth>800)floatCard.style.transform=`translateY(${Math.min(18,scrollY*.03)}px)`});
const roles=['IT Support','Systems Admin','Field Technician','Service Desk','Network Support','IT Infrastructure','Technical Support'];let ri=0;setInterval(()=>{ri=(ri+1)%roles.length;document.getElementById('exploreRole').textContent=roles[ri]},2400);
const themeToggle=document.getElementById('themeToggle');themeToggle.onclick=()=>document.body.classList.toggle('dark');
document.querySelectorAll('.download-link').forEach(a=>a.addEventListener('click',()=>{a.classList.add('downloading');setTimeout(()=>a.classList.remove('downloading'),800)}));
const navLinks=[...document.querySelectorAll('.navlinks a')];const sections=navLinks.map(a=>document.querySelector(a.getAttribute('href')));addEventListener('scroll',()=>{let idx=0;sections.forEach((s,i)=>{if(s&&s.getBoundingClientRect().top<140)idx=i});navLinks.forEach((a,i)=>a.classList.toggle('active',i===idx))});
document.querySelectorAll('.glow').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--mx',(e.clientX-r.left)+'px');card.style.setProperty('--my',(e.clientY-r.top)+'px')}));
const form=document.getElementById('contactForm'),success=document.getElementById('success');function validate(el){const small=el.parentElement.querySelector('small');let ok=el.value.trim().length>0;if(el.type==='email')ok=/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(el.value);el.classList.toggle('valid',ok);el.classList.toggle('invalid',!ok);small.textContent=ok?'':(el.type==='email'?'Enter a valid email address.':'This field is required.');return ok}form.querySelectorAll('input,textarea').forEach(el=>el.addEventListener('input',()=>validate(el)));form.addEventListener('submit',e=>{e.preventDefault();const els=[...form.querySelectorAll('input,textarea')],ok=els.every(validate);if(!ok)return;success.classList.add('show');const [name,email,message]=els.map(x=>x.value.trim());setTimeout(()=>{location.href=`mailto:Chauke.mld@gmail.com?subject=${encodeURIComponent('Portfolio enquiry from '+name)}&body=${encodeURIComponent('Name: '+name+'\nEmail: '+email+'\n\n'+message)}`},500)});
</script>
</body></html>
